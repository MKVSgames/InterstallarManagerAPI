class RocketOverall {
    constructor(demand, maxSpeed, agility) {
        this.demand = demand;
        this.maxSpeed = maxSpeed;
        this.agility = agility;
    }
}

module.exports = RocketOverall;